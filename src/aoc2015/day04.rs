extern crate uuid;

pub fn solve(input: &String) -> Vec<Result<usize, String>> {    
    let mut n = 1;
    // loop {
        // let a = format!("{}{}", input, n);        
        // let hash = Uuid::new_v5
        // if out_str.starts_with("00000") {break;}
        // n += 1;
    // }
        let hash = 1;
    vec!(Ok(hash))
}